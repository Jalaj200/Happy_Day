"""
Forever Us — Love App Configuration
"""

from django.apps import AppConfig


class LoveConfig(AppConfig):
    """Configuration for the Love application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "love"
    verbose_name = "Forever Us — Love"
